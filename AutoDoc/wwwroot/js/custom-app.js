function setSectionHeight() {
    console.log('uslo6');
    var screenHeight = window.innerHeight;
    //var baseHeight = 300; // Adjust this value based on your layout

    //// Calculate the height of the card-body and adjust the content height
    //var cardBody = document.querySelector('.card-body');
    //var basicDetailsHeight = 200; // Adjust this value based on your layout
    //if (cardBody) {
    //    basicDetailsHeight = cardBody.offsetHeight;
    //}

    //// Calculate the available height for the content
    //var contentFullHeight = screenHeight;
    //var contentReducedHeight = screenHeight - (baseHeight + basicDetailsHeight);
    //var navigationListHeight = screenHeight - baseHeight - 100; // Adjust 100px based on your layout

    //document.documentElement.style.setProperty('--base-height', baseHeight + 'px');
    //document.documentElement.style.setProperty('--basic-details-height', basicDetailsHeight + 'px');
    //document.documentElement.style.setProperty('--content-full-height', contentFullHeight + 'px');
    //document.documentElement.style.setProperty('--content-reduced-height', contentReducedHeight + 'px');
    //document.documentElement.style.setProperty('--navigation-list-height', navigationListHeight + 'px');

}

//window.addEventListener('resize', setSectionHeight);
//window.addEventListener('load', setSectionHeight);
