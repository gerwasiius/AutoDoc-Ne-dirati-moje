# AutoDoc

This repository contains a Blazor Server frontend (`AutoDoc` folder) and a backend API (`AutoDocService` folder). The frontend uses code-behind `.razor.cs` files for all business logic and dedicated services for HTTP communication.
